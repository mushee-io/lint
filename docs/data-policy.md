# Data minimization

Market Lint stores normalized public market/protocol data, organization configuration, technical usage records, and integrity/provenance metadata. It should not collect trader personal information unless a future, explicitly scoped feature requires it. API keys and connection secrets must be hashed or encrypted and never appear in logs.
