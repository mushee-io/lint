import { bad,ok } from "@/lib/api"; import { aggregate } from "@/lib/network"; import { events } from "@/lib/graph"; export function GET(_:Request,{params}:{params:Promise<{id:string}>}){return params.then(({id})=>events.some(e=>e.id===id)?ok(aggregate(id)):bad("Event not found",404))}
