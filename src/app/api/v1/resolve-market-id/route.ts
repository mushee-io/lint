import { bad,ok } from "@/lib/api"; import { markets } from "@/lib/graph"; import { universalId } from "@/lib/network"; export function GET(r:Request){const u=new URL(r.url),protocol=u.searchParams.get("protocol"),externalMarketId=u.searchParams.get("externalMarketId"),m=markets.find(x=>x.protocol===protocol&&x.externalId===externalMarketId);return m?ok(universalId(m)):bad("Market not found",404)}
