import Link from "next/link"; import { aggregate,consensus,universalId } from "@/lib/network"; import { markets } from "@/lib/graph";
export default function Network(){const id="event-eth-2026",a=aggregate(id),c=consensus(id);return <main className="mx-auto min-h-screen max-w-6xl border-x border-zinc-200 p-6 md:p-12"><Link href="/" className="text-sm underline">MARKET LINT</Link><p className="mt-14 font-mono text-xs tracking-widest text-zinc-500">NETWORK EFFECT / DEMO</p><h1 className="mt-4 text-6xl tracking-[-.07em]">Four venues. One event.</h1><p className="mt-6 max-w-2xl text-lg text-zinc-600">DEMO: Markets are normalized, assigned universal identities, and aggregated into auditable shared intelligence.</p><div className="mt-12 grid gap-4 md:grid-cols-4">{markets.filter(m=>m.canonicalEventId===id).map(m=><article key={m.id} className="border border-zinc-200 p-5"><p className="font-mono text-xs text-zinc-500">{m.protocol}</p><p className="mt-4 text-xl">{Math.round(m.prices[0]*100)}%</p><p className="mt-3 break-all text-xs text-zinc-500">{universalId(m).marketLintId}</p></article>)}</div><section className="mt-10 border-t border-zinc-950 pt-8"><p className="font-mono text-xs text-zinc-500">NETWORK CONSENSUS</p><p className="mt-2 text-7xl tracking-[-.07em]">{Math.round(c.consensusProbability*100)}%</p><p className="mt-2 text-zinc-600">{c.confidence} confidence · ${a.totalLiquidity.toLocaleString()} liquidity · {a.participatingProtocols.length} connected venues</p></section></main>}
