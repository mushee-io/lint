export type IntelligenceReport = { qualityScore: number; summary: string; ambiguityNotes: string[]; suggestedImprovements: string[]; resolutionSources: string[] };
export const stubReport: IntelligenceReport = { qualityScore: 72, summary: "The market has a clear directional premise, but its current wording leaves material room for disagreement at resolution. Define the measurement, the cut-off, and an authoritative source before publishing.", ambiguityNotes: ["The outcome does not define the measurement or threshold precisely.", "The resolution date lacks a time zone and an observation window.", "No hierarchy is specified if sources disagree."], suggestedImprovements: ["Name the exact metric and threshold that decide YES.", "State a final observation time in UTC.", "Add a primary source and a fallback source with precedence."], resolutionSources: ["Official government or regulator dataset", "Named exchange or issuer filing", "Methodology page for the referenced statistic"] };
